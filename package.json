{
  "private": true,
  "scripts": {
    "start": "npm --prefix mobile run web",
    "web": "npm --prefix mobile run web",
    "mobile:start": "npm --prefix mobile run start",
    "mobile:native": "npm --prefix mobile run native",
    "mobile:build": "npm --prefix mobile run build",
    "backend:start": "npm --prefix backend run start:dev",
    "backend:build": "npm --prefix backend run build"
  },
  "dependencies": {
    "@napi-rs/canvas": "^1.0.9",
    "pdfjs-dist": "^5.6.205"
  }
}
